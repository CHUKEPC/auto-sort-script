# Auto Sort Script

Автоматический скрипт для сортировки файлов в папке Downloads по типам.

## 📋 Описание

Скрипт автоматически отслеживает папку Downloads и сортирует загруженные файлы по категориям:
- **Images** - изображения (jpg, jpeg, png, gif, svg, bmp, webp)
- **Documents** - документы (pdf, docx, doc, txt, xlsx, pptx, csv)
- **Archives** - архивы (zip, rar, 7z, tar, gz)
- **Video** - видео (mp4, mov, avi, mkv, wmv)
- **Audio** - аудио (mp3, wav, flac, m4a)
- **Executables** - исполняемые файлы (exe, msi, dmg)

## ✨ Возможности

- ✅ Автоматическая сортировка файлов при загрузке
- ✅ Обработка дубликатов (автоматическое переименование)
- ✅ Мониторинг в реальном времени
- ✅ Кроссплатформенность (Windows, macOS, Linux)
- ✅ Легкая настройка категорий и расширений

## 🚀 Установка

### Требования

- Python 3.6 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/autosortscript.git
cd autosortscript
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

## 💻 Использование

### Базовое использование

Запустите скрипт:

```bash
python main.py
```

Скрипт начнет мониторинг папки Downloads и автоматически сортировать все новые файлы.

### Остановка скрипта

Для остановки скрипта нажмите `Ctrl+C` в терминале.

## ⚙️ Настройка

Вы можете настроить скрипт, изменив параметры в файле `main.py`:

### Изменение папки для мониторинга

```python
DOWNLOADS_DIR = os.path.expanduser("~/Downloads")  # По умолчанию
# Или укажите свою папку:
DOWNLOADS_DIR = "C:/Users/YourName/Downloads"
```

### Добавление новых категорий и расширений

```python
EXTENSION_MAP = {
    "Images": (".jpg", ".jpeg", ".png", ".gif", ".svg", ".bmp", ".webp"),
    "Documents": (".pdf", ".docx", ".doc", ".txt", ".xlsx", ".pptx", ".csv"),
    # Добавьте свою категорию:
    "Code": (".py", ".js", ".html", ".css", ".java"),
}
```

## 📁 Структура проекта

```
autosortscript/
├── main.py              # Основной скрипт
├── requirements.txt     # Зависимости проекта
├── README.md           # Документация
└── .gitignore          # Git ignore файл
```

## 🔧 Как это работает

1. Скрипт использует библиотеку `watchdog` для мониторинга файловой системы
2. При изменении папки Downloads (новый файл) срабатывает событие `on_modified`
3. Скрипт проверяет расширение файла и определяет категорию
4. Файл перемещается в соответствующую папку
5. Если файл с таким именем уже существует, создается уникальное имя (например: `file(1).jpg`)

## ⚠️ Важные замечания

- Скрипт не перемещает скрытые файлы (начинающиеся с точки)
- Скрипт не перемещает папки
- При перемещении файла делается пауза в 1 секунду для завершения записи на диск
- Убедитесь, что у скрипта есть права на чтение и запись в папке Downloads

## 🐛 Решение проблем

### Скрипт не перемещает файлы

- Проверьте права доступа к папке Downloads
- Убедитесь, что файл полностью загружен перед перемещением
- Проверьте логи в консоли на наличие ошибок

### Файлы перемещаются слишком быстро

- Увеличьте значение `time.sleep()` в методе `move_file()` (строка 60)

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👤 Автор

**CHUKEPC**
- GitHub: [@CHUKEPC](https://github.com/CHUKEPC)
- Telegram: [@ArtyomRoyal](https://t.me/ArtyomRoyal)

---

<div align="center">

⭐ Если проект был полезен, поставьте звезду! ⭐

</div>